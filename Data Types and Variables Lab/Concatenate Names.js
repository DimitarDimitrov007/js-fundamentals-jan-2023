function concatenationOfNames(nameOne, nameTwo, delimiter) {
    console.log(`${nameOne}${delimiter}${nameTwo}`);

}
concatenationOfNames('John', 'Smith', '->');
concatenationOfNames('Jan',
    'White',
    '<->'
);
concatenationOfNames('Linda',
'Terry',
'=>'
);
